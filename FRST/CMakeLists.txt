# The order of these is important.
# Each one creates a lib, and all the dependencies must come before it.
add_subdirectory(Interactions)
# add_subdirectory(WorkForce)
# The full executable here.
add_subdirectory(FRST)
